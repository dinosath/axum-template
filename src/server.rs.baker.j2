{% if protocol=='rest' -%}
use axum::Router;
{% else -%}
use tonic::transport::Server;
{% endif -%}
use log::{info, {{ 'error' if use_sqlx_migrations }}};
{% if database=='postgres' -%}
use sea_orm::{Database, DatabaseConnection};
{{ 'use sqlx::postgres::PgPoolOptions;' if use_sqlx_migrations -}}
{% endif -%}
use std::{env, error::Error};
{% if protocol=='rest' -%}
use tokio::net::TcpListener;
use crate::app_state::AppState;
{% endif -%}
{% if authentication=='jwt'-%}
use crate::services::auth::setup_auth_state;
use axum_gate::prelude::{Gate, Role, Group};
use std::sync::Arc;
{% endif -%}
{% if static_includes | length > 0 -%}
use static_serve::embed_assets;

{% for entry in static_includes -%}
embed_assets!("{{ entry.assets_dir }}", compress = true, strip_html_ext = true);
{% endfor -%}
{%- endif %}
{% if protocol=='grpc' -%}
{% for entity_name,entity in entities | items -%}
{% if entity.attributes | items %}
{% set message = entity_name | pascal_case -%}
use crate::services::{{ project_name | snake_case }}_service::{
    {{ message }}ServiceImpl,
    {{ message }}ServiceServer,
};
{% endif -%}
{% endfor -%}
{% endif -%}

{% if database=='postgres'-%}
pub async fn init_db() -> Result<DatabaseConnection, Box<dyn std::error::Error>> {
    let database_url = env::var("DATABASE_URL").unwrap_or_else(|_| "postgres://postgres:postgres@localhost:5432".to_string());
    let db: DatabaseConnection = Database::connect(&database_url).await?;
    {% if use_sqlx_migrations -%}
    info!("🔄 Running database migrations...");
    let pool = PgPoolOptions::new().max_connections(5).connect(&database_url).await?;
    if let Err(e) = sqlx::migrate!("./migrations").run(&pool).await { error!("❌ Failed migrations: {e}"); return Err(e.into()); }
    info!("✅ Database migrations completed successfully!");
    {% else %}
    db.get_schema_registry("{{project_name}}::models::*").sync(&db).await?;
    {% endif -%}
    Ok(db)
}
{% endif -%}

pub async fn run() -> Result<(), Box<dyn Error>> {
    dotenvy::dotenv().ok();
    env_logger::Builder::from_env(env_logger::Env::default().default_filter_or("info")).init();

    {% if database=='postgres'-%}
    let db = init_db().await?;
    {% endif -%}

    let port_env = env::var("PORT").unwrap_or_else(|_| "8080".to_string());
    let requested_port: u16 = port_env.parse().unwrap_or(8080);

{% if protocol=='rest' -%}
    let listener = TcpListener::bind(format!("[::]:{}", requested_port)).await?;
    let actual_port = listener.local_addr()?.port();
    info!("Server starting on port {}", actual_port);

    {% if authentication=='jwt' -%}
    let auth_state = setup_auth_state().await;
    {% endif -%}
    let state = AppState {
        {{- 'db,' if database=='postgres' -}}
        {% if authentication=='jwt' -%}
        account_repo: auth_state.account_repo,
        secret_repo: auth_state.secret_repo,
        jwt_codec: auth_state.jwt_codec,
        cookie_template: auth_state.cookie_template,
        {% endif -%}
    };
    let app = Router::new()
        {{ '.merge(static_router())' if static_includes | length > 0 }}
        {{ '.nest("/auth", crate::services::auth::auth_routes())' if protocol=='rest' and authentication=='jwt' }}
        {{ '.nest("/auth", crate::services::oidc::auth_routes())' if protocol=='rest' and authentication=='oidc' }}
        {% if authentication=='jwt' -%}
        .nest("/api", crate::controllers::routes()
            .layer(
                Gate::bearer::<_, Role, Group>("{{ project_name }}", Arc::clone(&state.jwt_codec))
                    .require_login()
            ))
        {% else -%}
        .nest("/api", crate::controllers::routes())
        {% endif -%}
        .with_state(state);

    axum::serve(listener, app).await?;
{% else -%}
    let addr = format!("[::]:{}", requested_port).parse()?;
    info!("gRPC Server starting on {}", addr);

    Server::builder()
        {% for entity_name,entity in entities | items -%}
        {% if entity.attributes | items %}
        {% set message = entity_name | pascal_case -%}
        .add_service({{ message }}ServiceServer::new({{ message }}ServiceImpl::new(db.clone())))
        {% endif -%}
        {% endfor -%}
        .serve(addr)
        .await?;
{% endif -%}
    Ok(())
}
