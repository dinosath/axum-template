[env]
# Show test output immediately
RUST_TEST_NOCAPTURE = "1"
# Show backtraces on panic
RUST_BACKTRACE = "1"

[alias]
# Run tests with immediate output
t = "test -- --nocapture"
tf = "test -- --nocapture --test-threads=1"

# Faster incremental builds
[build]
incremental = true
